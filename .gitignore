# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env — never commit any .env variant (secrets live here).
# .env.example is the tracked, secret-free template.
.env
.env.*
!.env.example

# Unpublished drafts / private inputs (Dan's .tex lives here).
# Anchored /data covers both a real dir and the symlink into ~/data/pat-helper/.
/data

# Editor
.DS_Store
.vscode/
.idea/

# Notebook checkpoints
.ipynb_checkpoints/
.worktrees/
